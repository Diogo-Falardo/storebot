version: "3.8"

services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ./web/.env
    environment:
      NODE_ENV: production
    networks:
      - kira-network

networks:
  kira-network:
    driver: bridge
